// Engineer-name aliases moved to @opencall/shared so the backend Final-EOD
// freeze resolves names exactly like the live dashboard. Re-export only.
export { ENGINEER_NAME_ALIASES, canonicalEngineerName } from "@opencall/shared";
